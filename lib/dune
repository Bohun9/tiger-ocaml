(library
  (name tiger_ml)
  (preprocess (pps ppx_deriving.ord ppx_deriving.show))
  (libraries ocamlgraph)
 )

(menhir
  (modules parser)
  (flags --explain --dump)
  (infer true)
)

(ocamllex lexer)
